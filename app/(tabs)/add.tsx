import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const add = () => {
  return (
    <View className="min-h-screen w-full flex-1 items-center bg-slate-600">
      <Text>add</Text>
    </View>
  );
};

export default add;

const styles = StyleSheet.create({});
